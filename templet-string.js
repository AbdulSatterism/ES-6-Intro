const priya = 'Asif Akbar';
const meye = "Meye tumi ki dukko cheno an";
const kobita = `Kobita tumi sopno chino na`;
// carret is the most powerful element in class es6
const multiLIne = `this is one kind of string 
this is anonther line`
// console.log(multiLIne)



const friends = ['abul', 'kaub', 'halel'];
const new1 = `<h3 class="friend-name">friend-${friends[0]}</h3>`;
// console.log(new1)

const first = 'Mamau';
const last = 'Khan Dhan';
const oldVersion = first + ' ' + last;
const newName = `${first} ${last}`
// const newName = `${first} ${last, friends[0]}`
console.log(newName)