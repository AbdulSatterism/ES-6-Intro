// variable
var deposit = 40;
// condition
if (deposit > 50) {

}
if (deposit < 30) {

}

// array
const numbers = [120, 45, 12, 48, 2];
const numberCount = numbers.length;
numbers.push(545)
numbers.pop()

// loop
// while and for 
for (const number of numbers) {

}
// function
function getFullName(first, second) {
    const name = first + ' ' + second;
    return name;
}
// console.log(getFullName('rohim','ali'));

// Object
const bottle = { color: 'yellow', contain: 'water' }

